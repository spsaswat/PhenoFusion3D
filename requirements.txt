# PhenoFusion3D -- Python dependencies to run the application
# (aligned with pyproject.toml [project] and optional capture extras).
# Requires: Python >=3.10
#
# ROS (rospy, sensor_msgs, etc.) is NOT listed here -- install from your
# ROS distro on lab Linux and use a venv with --system-site-packages
# (see install/install_linux.sh).

# --- Core: GUI, reconstruction, quality pipeline ---
open3d>=0.17.0
# Headless build on purpose (opencv-python's bundled Qt plugins break
# the PyQt5 GUI; the app never uses cv2's GUI functions).
opencv-python-headless>=4.8.0
numpy>=1.26.0
natsort>=8.4.0
tqdm>=4.66.0
PyQt5>=5.15.0
pyqtgraph>=0.13.0
matplotlib>=3.8.0

# --- Camera capture (RealSense USB; Windows + pip-based Linux) ---
# Omit this line only if you will never use in-app capture and only load
# RGB-D folders from disk. Not available as a prebuilt wheel on all ARM
# platforms -- use system librealsense + docs in that case.
#
# Pinned exactly, not floored: pyrealsense2 >= 2.55 cannot see an L515 at
# all, and the wheel must match the machine-wide librealsense SDK, which
# this project pins to 2.54.2. Wheels for 2.54.2.5684 exist for Python
# 3.10 and 3.11 only. See docs/L515_SETUP.md.
pyrealsense2==2.54.2.5684

# --- Developer tooling (optional) ---
# pip install -e ".[dev]"   or uncomment:
# pytest>=7.4.0
# ruff>=0.4.0
