# Known-good dependency pins for PhenoFusion3D.
# Captured from a verified working install (Ubuntu 20.04, Python 3.11,
# 2026-08-20).
#
# setup.sh passes this file to pip as a constraints file (-c). Constraints
# never ADD packages; they only pin the version of anything that does get
# installed, so a fresh clone reproduces the tested environment instead of
# whatever happens to be newest on PyPI that day. If the pins cannot be
# resolved (e.g. a different Python version or platform), setup.sh warns
# and retries without them, so this file can never make setup fail.
#
# To refresh after deliberately upgrading dependencies: verify the app runs,
# then update the pins from `pip freeze` and keep this header.

numpy==2.4.6
open3d==0.19.0
opencv-python==5.0.0.93
opencv-python-headless==5.0.0.93
natsort==8.4.0
tqdm==4.70.0
PyQt5==5.15.11
PyQt5-Qt5==5.15.19
PyQt5_sip==12.19.0
pyqtgraph==0.14.0
matplotlib==3.11.1
pillow==12.3.0
scipy==1.17.1
scikit-learn==1.9.0

# 2.54.x is the only SDK series supporting all project cameras
# (L515 support removed in 2.55+; D435/D405 fully supported in 2.54).
pyrealsense2==2.54.2.5684
